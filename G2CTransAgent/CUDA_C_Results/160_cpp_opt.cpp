// Optimized code(id = 160): 

void Copy_List(const int element_numbers, const float* origin_list, float* list) {
    for (int i = 0; i < element_numbers; ++i) {
        list[i] = origin_list[i];
    }
}